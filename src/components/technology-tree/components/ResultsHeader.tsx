
import React from "react";
import { FilterSort } from "../FilterSort";

export const ResultsHeader = () => {
  // This component is now redundant as we've moved its functionality to TabContent
  return null;
};
