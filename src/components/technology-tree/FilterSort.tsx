
import React from "react";
import { cn } from "@/lib/utils";

interface FilterSortProps {
  className?: string;
}

export const FilterSort = ({ className }: FilterSortProps) => {
  // This component is now superseded by the controls in TabContent
  return null;
};
